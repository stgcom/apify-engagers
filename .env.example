# Apify — Stage 1 (extraction)
APIFY_TOKEN=

# Exa — Stage 3 (URN -> public /in/ slug resolution)
EXA_API_KEY=

# GetLeads — Stage 4 tier 1 (cheapest, run first)
GETLEADS_API_KEY=

# Apollo — Stage 4 tier 2 (only for GetLeads misses)
APOLLO_API_KEY=

# Prospeo — Stage 4 tier 3 (only for Apollo misses)
PROSPEO_API_KEY=

# MillionVerifier — Stage 5 (email validation; never skip)
MILLIONVERIFIER_API_KEY=
